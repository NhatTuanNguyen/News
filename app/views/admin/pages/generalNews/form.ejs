<% 
	const collection = "general-news";
	const linkPrefix = systemConfig.prefixAdmin + `/${collection}/`;
%>

<% include ./../../elements/error %>

<div class="card card-info card-outline">
	<form action="<%= linkPrefix %>save/" method="POST" class="mb-0" >
		<div class="card-body">
		
			<div class="form-group row">
				<label for="link" class="col-sm-2 col-form-label text-sm-right required">Link</label>
				<div class="col-xs-12 col-sm-8">
					<input type="text" name="link" value="<%= item.link %>" class="form-control form-control-sm">
				</div>
			</div>

			<div class="form-group row">
				<label for="ordering" class="col-sm-2 col-form-label text-sm-right required">Ordering</label>
				<div class="col-xs-12 col-sm-8">
					<input type="number" name="ordering" value="<%= item.ordering %>" class="form-control form-control-sm">
				</div>
			</div>

			<div class="form-group row">
				<label for="status" class="col-sm-2 col-form-label text-sm-right">Status</label>
				<div class="col-xs-12 col-sm-8">
					<select name="status" class="custom-select custom-select-sm">
						<option value="novalue">Choose Status</option>
						<option value="active" <%= (item.status == "active") ? 'selected="selected"' : '' %> >Active</option>
						<option value="inactive" <%= (item.status == "inactive") ? 'selected="selected"' : '' %> >Inactive</option>
					</select>
				</div>
			</div>
            <div class="form-group row">
				<label for="category" class="col-sm-2 col-form-label text-sm-right required">Category</label>
				<div class="col-xs-12 col-sm-8">
					<input type="text" name="category" value="<%= item.category %>" class="form-control form-control-sm name_slug">
				</div>
			</div>
			<div class="form-group row">
				<label for="source" class="col-sm-2 col-form-label text-sm-right required">Source</label>
				<div class="col-xs-12 col-sm-8">
					<input type="text" name="source" value="<%= item.source %>" class="form-control form-control-sm name_slug">
				</div>
			</div>
			<div class="form-group row">
				<label for="slug" class="col-sm-2 col-form-label text-sm-right required">slug</label>
				<div class="col-xs-12 col-sm-8">
					<input type="text" name="slug" value="<%= item.slug %>" class="form-control form-control-sm">
				</div>
			</div>
		</div>
		<div class="card-footer">
			<div class="col-12 col-sm-8 offset-sm-2">
				<input class="form-control" name="id" value="<%= item.id %>" type="hidden" >
				<button type="submit" class="btn btn-sm btn-success mr-1"> Submit</button>
				<a href="<%= linkPrefix %>" class="btn btn-sm btn-danger mr-1"> Cancel</a>
			</div>
		</div>
	</form>
</div>